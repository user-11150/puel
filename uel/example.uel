import socket: -> Python

var sock = socket.socket();

sock.bind -> Call -> (("127.0.0.1",2500))
sock.listen -> Call -> (128)

while True:
    var conn, addr = sock.accept()
    var user_send = conn.recv(1024)
    print -> Call -> (user-send)
